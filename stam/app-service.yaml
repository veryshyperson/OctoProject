apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: nodejs-app
  ports:
    - protocol: TCP
      port: 3000      # Internal port for the service
      targetPort: 3000  # The container's port the app is running on
  type: ClusterIP    # Exposes the service only within the cluster
